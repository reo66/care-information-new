<% provide(:title, "#{@user.name} 編集") %>
<% provide(:class_text, 'user--edit') %>
<% provide(:button_text, "更新後未確認") %>
<h1><%="#{@user.name}さん 編集ページ"%></h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
  </div>
</div>
