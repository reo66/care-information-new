<% provide(:title, @user.name) %>
<div>
  <table class="table table-bordered table-condensed user-table">
    <tr>
      <th>name</th>
      <td><%= @user.name %></td>
    </tr>
    <tr>
      <th>department</th>
      <td><%= @user.department  %></td>
    </tr>  
    <tr>
      <th>unconfirmed</th>
      <td></td>
    </tr> 
  </table>
  <%= link_to "利用者一覧", login_path, class: "btn btn-md btn-primary" %>
  <%= link_to  "新利用者作成", signup_path, class: "btn btn-md btn-primary" %>
</div>