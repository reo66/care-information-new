<% provide(:title, '利用者検索結果') %>

<h1>検索結果</h1>

<div class="col-md-10 col-md-offset-1">
  <table class="table table-sm table-hover" id="table-care_users">
    <thead>
      <tr>
<th>image</th>
        <th><%= CareUser.human_attribute_name(:name) %></th>
        <th><%= CareUser.human_attribute_name(:department) %></th>
        <th><%= CareUser.human_attribute_name(:cuser_confirm) %></th>
    </thead>

    <% @results.each do |care_user| %>
    <tbody>
      <tr>
      <td>
      <% if care_user.image.present? %>
      <img src='<%= care_user.image %>'class="index_icon"></td>
      <% else %>
      <%= image_tag src='picture-3651039_1920.png', class: "index_icon", alt: "ユーザーアイコン" %>
      <% end %>
        <td>
          <% if current_user.admin? %>
            <%= link_to care_user.name, care_user %>
          <% else %>
            <%= care_user.name %>
          <% end%>
        </td>
        <td><%= care_user.department %></td>
        <td><%= check_box_tag :cuser_confirm %></td>
      </tr>
    <% end %>
        <tbody>
  </table>


<p><%= link_to "一覧に戻る",care_users_path, class: "btn btn-primary " %></p>
