<% provide(:title, '利用者検索結果') %>

<h1>検索結果</h1>

<div class="text">
  <p>
    未確認or新規 →
      <button class="btn-primary btn-sm" disabled>確認</button>
    　　確認後更新 →
      <button button class="btn-success btn-sm" disabled>確認</button>
  </p>
</div>


<%#--if @results.present? --#%>
<%#--  <p><%= "更新された利用者が#{@count.count}名います。再度確認をお願いします"%>
<%# </p> %>
<%#--  <% end --#%>
<h5>
<% if @count.count > 0 %>
  <p class="sign"><%= "更新された利用者が#{@count.count}名います。再度確認をお願いします"%></p>
<% end %>
</h5>

<div class="col-md-10 col-md-offset-1">
  <table class="table table-sm table-hover" id="table-care_users">
    <thead>
      <tr>
        <th></th>
        <th>name</th>
        <th>grade</th>
        <th>department</th>        
        <th>confirmation</th>

      </tr>
    </thead>
    <% @results.each do |care_user| %>
      <tbody>
        <tr>
          <td>  
          <% if care_user.image.present? %>
            <img src='<%= care_user.image %>'class="index_icon"></td>
          <% else %>
            <%= image_tag src='picture-3651039_1920.png', class: "index_icon", alt: "ユーザーアイコン" %>
          <% end %>
          <td><%= link_to care_user.name, care_user %></td>
          <td><%= care_user.grade %></td>
          <td><%= care_user.department %></td>
          <td>
            <div class="index_btn">
              <%= form_with(model: Intermediate, url: user_intermediates_path(current_user), local: true) do |form| %>
                <%= hidden_field_tag :user_id, current_user.id %>
                <%= hidden_field_tag :care_user_id, care_user.id %>
                <%= hidden_field_tag :confirmation, true %>
                <%= hidden_field_tag :indication, "" %>
                <% if Intermediate.exists?(user_id: current_user,care_user_id: care_user.id,confirmation: true) %>
                  <%= form.submit "確認済", disabled: true,class: "btn btn-default " %>
                <% elsif Intermediate.exists?(user_id: current_user,care_user_id: care_user.id, confirmation: false, indication: "更新") %>
                  <%= form.submit "確認", class: "btn btn-success " %>
                <% else %>
                  <%= form.submit "確認", class: "btn btn-primary " %>
                <% end %>
              <% end %>
            </div>
          </td>
        </tr>
      <tbody>
    <% end %>
  </table>


<p><%= link_to "back",care_users_edit_index_user_path(current_user), class: "btn btn-default " %></p>
